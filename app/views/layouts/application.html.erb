<!DOCTYPE html>
<html>
  <head>
    <title>DescontoInss</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="#">Desconto INSS</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <%= link_to "Relatórios", root_path, method: :get, class:"nav-link"  %>
          </li>
          <li class="nav-item">
           <%= link_to "Proponentes", proponente_index_path, method: :get, class:"nav-link"  %>
          </li>
        </ul>
      </div>
    </nav>
    <% if notice.present? %>
      <div id="error_explanation" data-turbo-cache="false" class="alert alert-secondary container login-container">
        <%= notice %>
      </div>
    <% end %>
    <% if alert.present? %>
      <div id="error_explanation" data-turbo-cache="false" class="alert alert-danger container login-container">
        <%= alert %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
